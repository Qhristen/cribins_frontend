import Container from '@/components/container';
import SubscriptionClient from '@/components/subscription';

const Subscription = () => {
  return (
    <Container>
      <div className="mt-20">
        <SubscriptionClient />
      </div>
    </Container>
  );
};

export default Subscription;
